---
title: "turbo code"
date: "2026-06-04"
category: "snippet"
subcategory: "web"
description: "turbo code from burp"
---

### source

```python
def queueRequests(target, wordlists):
    engine = RequestEngine(endpoint=target.endpoint,
                           concurrentConnections=100, # 100개의 연결을 동시에 맺음
                           requestsPerConnection=1,
                           pipeline=False
                           )

    # 1부터 100까지의 숫자를 큐에 대기
    for i in range(1, 101):
        engine.queue(target.req, str(i), gate='race_window')
    engine.openGate('race_window')

#결과 확인 함수
def handleResponse(req, result):
    if result.response.status == 200:
        table.add(req)
```